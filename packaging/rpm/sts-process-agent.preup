#!/bin/sh

PIDFILE=/opt/sts-process-agent/run/sts-process-agent.pid

# Source functions library.
. /etc/init.d/functions

echo "Stopping sts-process-agent (using killproc): "
killproc -p $PIDFILE -d 30
echo
return 0
